// IMPLEMENT: General utility functions
// - cn(...classes): Tailwind class merging helper using clsx + tailwind-merge (standard shadcn pattern)
// - formatBytes(bytes): converts byte count to human-readable string (e.g., "1.2 MB")
// - formatNumber(n): formats large numbers with commas (e.g., 1000000 → "1,000,000")
// - formatDate(date): formats a Date to a readable string (e.g., "Mar 15, 2026")
// - truncate(str, maxLength): truncates a string and adds ellipsis
// - sleep(ms): async sleep helper for retry logic
