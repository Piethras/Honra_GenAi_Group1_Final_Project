// IMPLEMENT: Report HTML builder
// - buildReportHTML(title, queries, insights): generates a branded, print-ready HTML string
// - Include the app logo, report title, generated date in the header
// - For each insight: render title, description, confidence badge, chart placeholder
// - For each query: render the question text, answer text, and result data table
// - Use inline CSS only (no external stylesheets) so Puppeteer renders correctly
// - Apply professional typography: clear headings, readable table styles, page breaks
