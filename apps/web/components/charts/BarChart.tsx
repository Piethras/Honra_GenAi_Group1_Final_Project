// IMPLEMENT: Recharts BarChart wrapper
// - Props: data (Row[]), xKey (string), yKey (string | string[]), title (string)
// - Render a Recharts BarChart with ResponsiveContainer
// - Support grouped bars if yKey is an array
// - Include Tooltip with formatted values and CartesianGrid for readability
// - Apply brand colors from the CSS theme variables
