// IMPLEMENT: Dataset statistics card (Client Component)
// - Props: label (string), value (string | number), icon (ReactNode), trend (optional: { value, direction })
// - Render a card with an icon, large value, and label
// - If trend is provided, show a green/red indicator with percentage change
// - Used on the dashboard home to show total datasets, queries, etc.
