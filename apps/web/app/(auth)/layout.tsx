// IMPLEMENT: Auth layout
// - Centered single-column layout for auth pages (login, register, onboarding)
// - Show the app logo at the top
// - Restrict to unauthenticated users only — redirect signed-in users to /dashboard
