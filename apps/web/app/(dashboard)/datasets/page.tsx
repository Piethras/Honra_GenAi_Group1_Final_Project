// IMPLEMENT: Datasets list page
// - Fetch and display all datasets belonging to the current user/org
// - Show each dataset as a card: name, file type, row count, upload date, status badge
// - Status badge colors: PROCESSING (yellow), READY (green), ERROR (red)
// - Include a drag-and-drop upload zone at the top (or prominent Upload button)
// - Add search/filter bar to filter datasets by name or file type
// - Clicking a dataset navigates to /datasets/[id]
// - Delete dataset button with confirmation dialog
