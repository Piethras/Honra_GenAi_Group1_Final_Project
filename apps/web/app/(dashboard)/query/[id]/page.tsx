// IMPLEMENT: Individual query result page (shareable URL for a specific result)
// - Fetch the query record by ID from the DB
// - Display: question text, plain-English answer, chart, result data table
// - Include "Show generated code" collapsible section (shows the Pandas code)
// - "Add to Report" button to include this result in a report
// - "Ask a follow-up" button that opens the query interface with the same dataset pre-selected
