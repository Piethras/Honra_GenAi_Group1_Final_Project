// IMPLEMENT: NLQ (Natural Language Query) interface — main query page
// - If datasetId is in URL params, pre-select that dataset in the selector
// - Dataset selector dropdown (shows user's READY datasets)
// - Chat-style input area with a text field and Submit button
// - Query history panel on the right or below showing previous questions
// - Each history item shows: question text, chart thumbnail, timestamp
// - Clicking a history item re-renders that result
// - Empty state with example questions to inspire users
