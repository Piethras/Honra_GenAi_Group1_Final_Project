// IMPLEMENT: Global error boundary (Client Component)
// - Must be a 'use client' component
// - Display a user-friendly error message — never show raw stack traces
// - Include a "Try again" button that calls the reset() function passed as prop
// - Log the error to Sentry for tracking
