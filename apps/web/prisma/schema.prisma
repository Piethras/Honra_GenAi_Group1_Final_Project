// IMPLEMENT: Full Prisma schema — single source of truth for all DB tables
// Copy the complete schema from Section 3.1 of the Build Roadmap (pages 8–10):
//   - User model (linked to Clerk via clerkId)
//   - Organization model
//   - OrgMember model (userId + orgId + role)
//   - Dataset model (fileUrl, schema JSON, status enum)
//   - Query model (questionText, generatedCode, resultJson, chartConfig, answerText)
//   - Insight model (title, description, type, confidence, chartConfig)
//   - Report model (title, exportUrl, format, content JSON)
//   - Subscription model (Stripe customer + subscription IDs, status, plan)
// Enums: Plan (FREE/PRO/TEAM/ENTERPRISE), OrgRole (ADMIN/ANALYST/VIEWER),
//        DataStatus (PROCESSING/READY/ERROR), QueryStatus (PENDING/RUNNING/SUCCESS/ERROR)
// After editing this file, run: npx prisma migrate dev --name <migration_name>
